"use strict";
function printAll(str) {
    if (typeof str === 'object') {
        // for(const s of str){
        //     console.log(str)
        // }
    }
    else if (typeof str === 'string') {
        console.log(str);
    }
    else {
        // ...
    }
}
